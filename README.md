# This repo is Proof of concept of how to use select and owl carroussel with next.js

Have a look at index.tsx

One must make sure that the dom if fully load before registering select and owl carrousel event
